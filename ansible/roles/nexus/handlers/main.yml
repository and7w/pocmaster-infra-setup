---
# handlers file for nexus

- name: "start nexus"
  become: true
  service: name=nexus state=started
  tags:
    - init

- name: "restart nexus"
  become: true
  service: name=nexus state=restarted

- name: "reload systemd"
  become: true
  systemd:
    daemon_reload: yes
